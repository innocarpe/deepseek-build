# Project surface (`.deepseek-build/`)

Runtime-facing project config for DeepSeek Build (committed examples only).

| Path | Role |
|------|------|
| `skills/` | Project-local Agent Skills |
| `agents/` | Custom agent definitions (later) |
| `workflows/` | Orchestration scripts (later) |

Never commit API keys. User secrets go in user home config (schema TBD in specs).
