[package]
name = "dsb-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "DeepSeek Build CLI (binaries: deepseek-build, dsb)"
publish = false

# Primary public command (ADR 0006).
[[bin]]
name = "deepseek-build"
path = "src/main.rs"

# Short alias — same entrypoint and SemVer.
[[bin]]
name = "dsb"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive", "env"] }
dsb-agent = { path = "../dsb-agent" }
dsb-config = { path = "../dsb-config" }
dsb-provider-deepseek = { path = "../dsb-provider-deepseek" }
dsb-tools = { path = "../dsb-tools" }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "io-std", "io-util"] }
anyhow = "1"

[lints]
workspace = true
