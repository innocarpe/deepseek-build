# Cut evidence — **`4.0.0`** / tag **`v4.0.0`**

**Date:** 2026-08-07  
**Story:** G008 Cut-4.0.0 · `fleet-4x`  
**Depends on:** `v3.0.0` hearts green

## Claimed vs shipped (PRD-v4 P0)

| # | Item | Status | Evidence |
|---|------|--------|----------|
| 1 | L3 capability matrix | **green** | [L3_CAPABILITY_MATRIX.md](../L3_CAPABILITY_MATRIX.md) |
| 2 | Product defaults: subagents on; yolo false | **green** | `agent_launch` seed + tests |
| 3 | Subagent / worktree / bg documented | **green** | user-guide 11–14 |
| 4 | Smoke without default mutation path | **green** | `./scripts/test-l3-smoke.sh --offline-only` |
| 5 | Heart regression path_a | **green** | `cargo test -p dsb-{tools,context,agent} path_a` |
| 6 | Tag `v4.0.0` | **this cut** | full SemVer `4.0.0` |

## Commands

```bash
./scripts/check-semver.sh
cargo test -p dsb-cli -- product_config_seed
cargo test -p dsb-tools path_a
cargo test -p dsb-context path_a
cargo test -p dsb-agent path_a
./scripts/test-product-offline.sh
./scripts/test-l3-smoke.sh --offline-only
```

## Honesty

- 3.0.0 = heart fusion Path A  
- 4.0.0 = L3 **product defaults + docs + matrix** (not YOLO, not multi-vendor)  
- Worktree remains **opt-in** CLI (`--worktree`); bare `dsb` still opens single-session TUI  
