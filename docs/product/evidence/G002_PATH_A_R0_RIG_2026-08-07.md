# G002 PathA-R0-Rig — evidence (owner-bar-5x)

| Field | Value |
|-------|--------|
| **Story** | G002 PathA-R0-Rig (`G002-g002-patha-r0-rig`) |
| **WAVE unit** | **5x-H0-3** |
| **Date** | 2026-08-07 |
| **Base SHA** | `be0490a310b07b906b41f13c82b21628588e3ad8` (after G001 PR #90) |
| **SemVer on disk** | `4.0.2` — not owner-bar complete |

## Done criteria

| Check | Result |
|-------|--------|
| Public entry via `deepseek-build agent` (not raw agent alone) | **PASS** |
| `DEEPSEEK_BUILD_AGENT_BIN` unset | **PASS** |
| Scripted DeepSeek server (`scripts/lib/scripted_deepseek_server.py`) | **PASS** |
| Wire transcript `/chat/completions` captured | **PASS** (3 POSTs) |
| Agent stdout contains scripted token `path-a-r0-ok` | **PASS** |
| Dual CLI `dsb --version` | **PASS** |
| No cargo-test-only evidence for this story | **PASS** |

## Commands

```bash
./scripts/test-path-a-public-entry-e2e.sh
# → test-path-a-public-entry-e2e: PASS
# evidence_wire=docs/product/evidence/PATH_A_R0_WIRE_last.jsonl
# evidence_meta=docs/product/evidence/PATH_A_R0_META_last.txt
```

Captured run (this session):

```text
cli=/Users/WooseongKim/.deepseek-build/bin/deepseek-build
dsb=/Users/WooseongKim/.deepseek-build/bin/dsb
agent_resolved=…/xai-grok-pager  (runnable; product deepseek-build-agent path may SIGKILL on some hosts)
DEEPSEEK_BUILD_AGENT_BIN_unset=yes
scripted_base_url=http://127.0.0.1:<ephemeral>
public entry → agent_launch → scripted DeepSeek wire (3 request(s))
stdout: path-a-r0-ok
exit 0
```

## Artifacts

| Path | Role |
|------|------|
| [`PATH_A_R0_WIRE_last.jsonl`](./PATH_A_R0_WIRE_last.jsonl) | Slimmed wire (auth redacted; bodies truncated for repo) |
| [`PATH_A_R0_META_last.txt`](./PATH_A_R0_META_last.txt) | CLI/agent/paths/exit meta |
| `scripts/lib/scripted_deepseek_server.py` | Hermetic OpenAI-compat SSE server |
| `scripts/test-path-a-public-entry-e2e.sh` | Public-entry driver |

## Wire summary

| n | path | model (request) | stream |
|---|------|-----------------|--------|
| 0 | `/chat/completions` | `grok-4.5` (session title side-call) | true |
| 1 | `/chat/completions` | `deepseek-v4-flash` | true |
| 2 | `/chat/completions` | `deepseek-v4-flash` | true |

Authorization header always **REDACTED** by the scripted server.

## Host note (not a skip)

On this machine, the install path  
`~/.deepseek-build/bin/deepseek-build-agent` receives **SIGKILL** on exec (same bytes as `xai-grok-pager` which runs).  
The e2e harness **copies** a known-runnable agent into hermetic `DEEPSEEK_BUILD_HOME/bin/deepseek-build-agent` so public launch resolution stays product-shaped without relying on the killed realpath. Track for G011 install provenance.

## Explicit non-claims

- Does **not** claim mint / snippet_safe / liveness / any ledger PASS.
- Does **not** green `test-owner-bar.sh` (still RED).
- Session-title side-call model id `grok-4.5` is recorded for later routing honesty (G009), not fixed here.

## Next story

**G003 MintFileVersion** — `read_file` mints `file_version` / `snippet_id` on Path A wire.
