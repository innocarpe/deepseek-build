# VC013 — L3 Path A train cut **`5.4.0`**

| Field | Value |
|-------|--------|
| **Story** | **VC013** — `test+chore(release): 5.4.0 L3 cut` after VC010–VC012 Path A R0A (vision **V3** / Track C) |
| **Plan** | `vision-complete-5x` |
| **Date** | 2026-08-08 |
| **Status** | **READY** — hermetic L3 Path A R0A re-prove green; required gates green; live extended L3 matrix green; on-branch cut **`5.4.0`** landed; PR not merged |
| **SemVer** | on-branch **`5.4.0`** (this story is the **only** bump unit for the L3 minor) |
| **Depends on** | **VC012** worktree dogfood (open PR **#144** `vc012-worktree-dogfood` @ tip `32d8aad`) |
| **Board** | [`VISION_COMPLETE_5X_GOALS.md`](../VISION_COMPLETE_5X_GOALS.md) · DAG [`WAVE_5x_VISION_PR_DAG.md`](../WAVE_5x_VISION_PR_DAG.md) |
| **Normative** | V3-50-1/2 · V3-60-1/2 · V3-WT · SSOT version fields · ADR 0006 SemVer · release-cycle bump only (no publish/merge in-story) |
| **Prior** | VC010 multi-tool+bg R0A · VC011 subagent+worker-cache R0A · VC012 worktree dogfood R0A (all **READY**, unversioned on stack) |

**This file is the mandatory ultragoal PR unit plan for VC013 plus (later) cut evidence.**  
It does **not** claim vision freeze **`5.5.0`**, npm publish, or GitHub Release attach. It does **not** invent new L3 features beyond the cut of already-proven VC010–VC012 Path A evidence.

---

## 0. Floor and dependency facts

### 0.1 Live floor (story open; 2026-08-08 UTC)

| Probe | Live result |
|-------|-------------|
| This worktree branch | `vc013-5-4-cut` @ `/Users/WooseongKim/Projects/deepseek-build/vc013-5-4-cut` |
| Stack base for PR | **`vc012-worktree-dogfood`** (open PR **#144**); **not** `origin/main` until after #144 merges |
| Base tip (expected) | **`32d8aadd5d0550d410dd0da19bd20f7d73ba816d`** (VC012 final head) |
| Working tree HEAD at plan | **`32d8aad`** (matches base tip; no local delta yet) |
| `git show origin/main:Cargo.toml` version | **`5.2.2`** |
| `package.json` on `origin/main` | **`5.2.2`** |
| Working tree product version (stack tip) | **`5.3.0`** (VC006 Deep Code cut on stack; carried through VC007–VC012) |
| `npm view @innocarpe/deepseek-build version` | **`5.2.2`** |
| `gh release list` Latest | **`v5.2.2`** |
| Board DAG PR unit | VC013 — `test+chore(release): 5.4.0 L3 cut` |
| Board story-row residual wording | “Live extended matrix when key present” — **optional live L3.1+ when key loads**; hermetic Path A R0A re-prove is the fail-close bar |
| Stacked open train (L3) | #142 VC010 · #143 VC011 · #144 VC012 (all unmerged) |

### 0.2 Floor interpretation (fail-close)

- **Live shipped floor is `origin/main` / npm / GitHub Latest = `5.2.2`.**
- **Stack product version is already `5.3.0`** (VC006 Spec 45 Deep Code cut). Do **not** re-cut **`5.3.0`**.
- **`5.2.0` / `5.2.1` / `5.2.2` / `5.3.0` are used** on the train or ship floor. Next free **feature minor for this L3 cut is `5.4.0`**.
- Historical board mapping “Reasonix cut VC009 → 5.3.0” is **stale vs live stack** (VC006 already took 5.3.0). This story does **not** re-plan Track B; it only cuts Track C L3.
- **Open exactly one stacked PR** with base **`vc012-worktree-dogfood`** and body **`Depends on #144`**. **Do not merge.** **Do not publish npm / tag / Release** in this story (separate release lane after merge).
- Claims must remain **public Path A only** for behavior: hermetic re-runs of `test-path-a-vc010/011/012-r0a.sh` plus required gates. Thin Path B unit greens are not sole cut proof.
- **Live extended matrix** (`test-l3-smoke.sh` without `--offline-only`, optionally `--extended`) runs **only when** `load_deepseek_key` succeeds from `DEEPSEEK_API_KEY` or `~/.deepseek-build/credentials.json`. If live fails or is SKIP → record residual honestly; **do not invent live green**. Live is **not** a substitute for hermetic Path A R0A.
- If hermetic L3 R0A or required gates fail → **stop**; do not bump SemVer.
- **Do not cut `5.5.0`.** **Do not modify VC012 / PR #144.** **No Claude/Codex children.**

---

## 1. Why this PR (one sentence)

Re-prove the Grok L3 Path A R0A train (multi-tool+bg, subagent+worker-cache, worktree dogfood) under hearts on the public `deepseek-build`/`dsb` path, optionally record live extended smoke when a key is present, keep owner-bar / path-linkage / heart / SemVer gates green, and cut product **`5.4.0`** as the Track C packaging unit — without merging, publishing, or inventing new feature work.

---

## 2. Acceptance map (V3 → evidence)

| ID | Requirement | Evidence source (pre-cut) | Cut re-prove |
|----|-------------|---------------------------|--------------|
| **V3-50-1** | Multi-tool RO parallel + mutate serial Path A | VC010 R0A READY | `./scripts/test-path-a-vc010-r0a.sh` |
| **V3-50-2** | Background shell + collect-by-id Path A | VC010 R0A READY | same |
| **V3-60-1** | Explore + implement subagent Path A | VC011 R0A READY | `./scripts/test-path-a-vc011-r0a.sh` |
| **V3-60-2** | Worker reuses parent stable prefix (stamp) | VC011 R0A READY | same |
| **V3-60-3** | Worker mutation invalidates parent snippets | **VC011 residual** (honest) | restate residual; **not** blocker if already documented |
| **V3-WT** | Worktree dogfood + bare `dsb` honesty | VC012 R0A READY | `./scripts/test-path-a-vc012-r0a.sh` |
| **Gates** | owner-bar · path-linkage · heart · SemVer | prior READY | re-run all |
| **Live extended (optional)** | L3 smoke when key present | board story-row | `./scripts/test-l3-smoke.sh` (+ `--extended` if safe) |

### 2.1 Required gates (must stay green before bump)

| Gate | Command |
|------|---------|
| SemVer shape + npm match | `./scripts/check-semver.sh` |
| Path A linkage | `./scripts/check-path-a-linkage.sh` |
| Owner-bar | `./scripts/test-owner-bar.sh` |
| Heart regression | `./scripts/test-heart-regression.sh` |
| Hermetic L3 R0A | `test-path-a-vc010-r0a.sh` · `test-path-a-vc011-r0a.sh` · `test-path-a-vc012-r0a.sh` |

Restore generated TSV side-effects to HEAD; **do not commit** accidental gate TSV noise unless the evidence plan intentionally records results under `docs/product/evidence/`.

### 2.2 Release cut fields (unit 2 only; conditional)

Only after §2.1 hermetic PASS:

| Item | Value |
|------|--------|
| SemVer | **`5.4.0`** full form (never `5.4`) |
| Tooling | `./scripts/bump-version.sh 5.4.0 --desc "…"` (preferred) |
| Touch | `Cargo.toml` / `Cargo.lock` / `package.json` / `CHANGELOG.md` / README version literals / `docs/product/versions/README.md` as bump script updates |
| Non-cut | Do **not** run `release.sh` publish/merge/tag in this story |
| Non-reuse | Never claim packaging of **5.2.x–5.3.0** as this cut |

### 2.3 Explicit non-claims

- Not vision freeze / **`5.5.0`**
- Not npm publish / GitHub Release assets for **5.4.0** (post-merge release lane)
- Not closing VC011 residual **V3-60-3** Path A parent snippet expire unless separately green
- Not interactive TTY worktree **create** sole green (VC012 residual)
- Not inventing new L3 feature code beyond cut + evidence + honest residual docs

---

## 3. PR units (single stacked PR; atomic commits)

This story is **one PR** (task: exactly one unmerged stacked PR). Internally:

### PR unit 1 — `docs(product): VC013 5.4.0 L3 cut plan + floor`

- **Intent:** Plan-first gate; record live floor and acceptance map before any bump.
- **Touches:** `docs/product/evidence/VC013_L3_5_4_0_CUT_2026-08-08.md` (this file)
- **Depends on:** VC012 tip / #144
- **Parallelizable with:** none (first)
- **SemVer:** none
- **Tests:** none (docs only)

### PR unit 2 — `test+docs(product): re-prove L3 Path A matrix + optional live`

- **Intent:** Re-run hermetic VC010/011/012 R0A + required gates; attempt live L3 smoke when key loads; record results/residuals into evidence.
- **Touches:** evidence META/WIRE if re-run rewrites; this file READY sections; optional `_last_l3_smoke.tsv` or committed summary only if useful
- **Depends on:** unit 1
- **SemVer:** none yet
- **Tests:** §2.1 commands

### PR unit 3 — `chore(release): bump product to 5.4.0`

- **Intent:** Single SemVer bump for Track C L3 cut after re-prove green.
- **Touches:** version surfaces via `bump-version.sh`
- **Depends on:** unit 2 PASS
- **SemVer:** **`5.4.0`**
- **Tests:** `./scripts/check-semver.sh` · `./scripts/reorder-changelog.sh --check`

### PR unit 4 — `docs(product): VC013 READY evidence + independent review`

- **Intent:** Fill READY, residuals, gate table; land independent Grok review file with explicit residuals.
- **Touches:** this evidence file (READY), `VC013_INDEPENDENT_REVIEW_2026-08-08.md`
- **Depends on:** unit 3
- **SemVer:** none (already cut)
- **Tests:** static consistency with HEAD version **5.4.0**

## Sequential

1. unit 1 → unit 2 → unit 3 → unit 4 → open PR (no merge)

## Parallel

- None for this cut (single worker, single stack slot).

---

## 4. Stack / PR open checklist (implementer)

- Branch: `vc013-5-4-cut`
- Base: `vc012-worktree-dogfood`
- Body: **`Depends on #144`**
- Labels: prefer **`chore`** + **`test`** (kind) and **`area/orchestrator`** (L3 train) — existing repo labels only
- English public text; run `gh-public-english-gate`
- Verify: `gh pr view --json title,labels,baseRefName,headRefName,url,mergeable`
- **Do not merge**

---

## 5. READY evidence

**Status: READY** (cut complete; independent review sibling).

### 5.0 Provenance

| Field | Value |
|-------|--------|
| Plan-first commit | `c3857a9` (`docs(product): VC013 5.4.0 L3 cut plan and floor`) |
| **R0A / cut source head** | **`96a9b3cb03ee5c2570146c6b0d96f49532fd35bc`** (`chore(release): bump product to 5.4.0`) |
| META `git_sha` (all VC010/011/012 scenarios) | **`96a9b3cb03ee5c2570146c6b0d96f49532fd35bc`** |
| Agent binary (stack release) | `third_party/grok-build/target/release/xai-grok-pager` sha256 `a56897aa2cdab00eb2b47a53796007d55bc4aa73c983c9fc59ffe9f7de370e54` |
| Docs-only tips after READY | do **not** require R0A re-run unless agent/CLI sources change |

### 5.1 Re-prove command log

| Command | Result | Notes |
|---------|--------|-------|
| `./scripts/test-path-a-vc010-r0a.sh` (full build) then `--skip-build` @ `96a9b3c` | **PASS** | multi-read-parallel, mixed-mutate-serial, bg-collect-by-id |
| `./scripts/test-path-a-vc011-r0a.sh --skip-build` @ `96a9b3c` | **PASS** | explore-subagent, implement-subagent-mutate, worker-cache-stamp |
| `./scripts/test-path-a-vc012-r0a.sh --skip-build` @ `96a9b3c` | **PASS** | cli-surface, flag-forward, opt-in-stamp, headless-no-create |
| `./scripts/check-semver.sh` | **PASS** | post-bump `5.4.0` cargo ≡ npm |
| `./scripts/check-path-a-linkage.sh` | **PASS** | |
| `./scripts/test-owner-bar.sh` | **PASS** | 60/60; TSV side-effect not committed |
| `./scripts/test-heart-regression.sh` | **PASS** | PATH_A_E2E SKIP default; L3 offline PASS |
| `./scripts/test-l3-smoke.sh --extended` | **PASS** | **live** L3.1–L3.3 + L3.5 via `credentials.json`; L3.0/L3.4 offline |
| `./scripts/reorder-changelog.sh --check` | **PASS** | |

### 5.2 Live extended matrix (board story-row)

| Probe | Result |
|-------|--------|
| Key load | `~/.deepseek-build/credentials.json` present (`api_key`); `DEEPSEEK_API_KEY` env unset — loader still found key |
| L3.0 help flags | **PASS** |
| L3.4 worktree help | **PASS** |
| L3.1 headless text | **PASS** (`pong`) |
| L3.2 background shell | **PASS** |
| L3.3 `--no-subagents` | **PASS** |
| L3.5 spawn_subagent explore (extended) | **PASS** (harness recorded OK; model may narrate tool-name confusion — stamp is harness PASS) |

Hermetic Path A R0A remains the fail-close cut bar. Live matrix is **additional** board residual closure, not a substitute.

### 5.3 Version after cut

| Field | Actual |
|-------|--------|
| `Cargo.toml` workspace version | **`5.4.0`** |
| `package.json` version | **`5.4.0`** |
| CHANGELOG | `## 5.4.0 — 2026-08-08` under Unreleased |
| versions decision log | row for **5.4.0** L3 cut (PR number filled at open) |

### 5.4 V3 acceptance at cut

| ID | Result at cut |
|----|----------------|
| **V3-50-1** | **PASS** — VC010 multi-read + mixed-mutate R0A @ `96a9b3c` |
| **V3-50-2** | **PASS** — VC010 bg-collect-by-id R0A @ `96a9b3c` |
| **V3-60-1** | **PASS** — VC011 explore + implement-class subagent R0A @ `96a9b3c` |
| **V3-60-2** | **PASS** — VC011 `worker_epochs_match=true` stamp @ `96a9b3c` |
| **V3-60-3** | **RESIDUAL** — Path A parent snippet expire not newly proven (carry VC011) |
| **V3-WT** | **PASS** — VC012 four-scenario R0A @ `96a9b3c` |

### 5.5 Residuals at READY

| Residual | Status |
|----------|--------|
| V3-60-3 Path A parent snippet expire | **Carry** VC011 residual |
| Interactive TTY worktree create sole green | **Carry** VC012 residual |
| npm / GitHub Release **5.4.0** publish | **Out of story** (post-merge release lane) |
| Vision freeze **5.5.0** / VC014–VC015 | **Out of story** |
| Main / npm still **5.2.2** until stack merges + publish | Expected; this PR is on-branch cut only |

### 5.6 Artifact pointers

| Artifact | Role |
|----------|------|
| `PATH_A_R0_VC010_*_META_last.txt` / `*_WIRE_last.jsonl` | Multi-tool + bg re-prove @ cut head |
| `PATH_A_R0_VC011_*_META_last.txt` / `*_WIRE_last.jsonl` | Subagent + worker cache re-prove @ cut head |
| `PATH_A_R0_VC012_*_META_last.txt` / related | Worktree dogfood re-prove @ cut head |
| `VC013_INDEPENDENT_REVIEW_2026-08-08.md` | Independent Grok review |

---

## 6. Implementer checklist

- [x] Floor re-check (`main` / npm / `gh release` / stack tip)
- [x] Plan-first file committed before implementation
- [x] Hermetic L3 R0A re-prove green
- [x] Required gates green
- [x] Live extended attempted (**PASS** with credentials)
- [x] Single bump to **`5.4.0`**
- [x] READY + independent review
- [ ] One stacked English labeled PR; **do not merge**
