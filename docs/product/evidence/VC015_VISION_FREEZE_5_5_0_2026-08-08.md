# VC015 — vision freeze audit / cut **`5.5.0`** (fail-close)

| Field | Value |
|-------|--------|
| **Story** | **VC015** — dual-review-ready vision freeze gate for **`5.5.0`** after VC014 docs pass |
| **Plan** | `vision-complete-5x` |
| **Date** | 2026-08-08 |
| **Status** | **PLAN** (this commit) — audit in progress; **no freeze claim** yet |
| **SemVer** | **none at plan open**; freeze path only may bump **exactly once** to **`5.5.0`** after all V1–V4 criteria are genuinely green |
| **Depends on** | **VC014** user-guide + KNOWN_LIMITS (open PR **#146** `vc014-vision-docs` @ **`58aff64`**) |
| **Board** | [`VISION_COMPLETE_5X_GOALS.md`](../VISION_COMPLETE_5X_GOALS.md) · DAG [`WAVE_5x_VISION_PR_DAG.md`](../WAVE_5x_VISION_PR_DAG.md) |
| **Normative** | VISION V1–V4 · Spec 45/10/60 · ADR 0006/0007/0009 · versioning + release-cycle (bump only; **no** tag/npm/GitHub publish in-story) |
| **Runtime** | **Grok only** — no Claude/Codex children |

**This file is the mandatory ultragoal PR unit plan for VC015 plus (later) freeze or readiness-audit evidence.**  
It does **not** invent residual green. It does **not** edit VC014 / PR **#146**. It does **not** merge. Dual adversarial Grok review lanes run **separately after** this branch is ready — this story does **not** self-count as dual review.

---

## 0. Floor and dependency facts (session open)

### 0.1 Live floor re-check (2026-08-08)

| Probe | Live result |
|-------|-------------|
| This worktree | `/Users/WooseongKim/Projects/deepseek-build/vc015-freeze-audit` |
| Branch | `vc015-freeze-audit` @ **`58aff64`** (matches `vc014-vision-docs` tip / PR **#146** head) |
| Stack base for PR | **`vc014-vision-docs`** (open PR **#146**); **not** `origin/main` until after #146 merges |
| Working tree product version (stack tip) | **`5.4.0`** (VC013 on-branch L3 cut; unmerged vs live) |
| `git show origin/main:Cargo.toml` version | **`5.2.2`** |
| `package.json` on `origin/main` | **`5.2.2`** |
| `npm view @innocarpe/deepseek-build version` | **`5.2.2`** |
| npm versions include | `… 5.2.0, 5.2.1, 5.2.2` (no `5.3.0`/`5.4.0`/`5.5.0` on registry) |
| `gh release list` Latest | **`v5.2.2`** |
| `gh release view v5.2.2` assets | **`deepseek-build-5.2.2-darwin-arm64.tar.gz`** present |
| Board freeze target | Prefer **`5.5.0`** if free |
| Next free feature minor | **`5.5.0`** (must never reuse **5.2.0–5.4.0**) |

### 0.2 Floor interpretation (fail-close)

1. **Live shipped floor** = `origin/main` / npm / GitHub Latest **`5.2.2`**.  
2. **Stack tip** already packages **`5.4.0`** (unmerged train). Do **not** re-cut **5.4.0**.  
3. **`5.2.0`–`5.4.0` are used** on ship line or stack. Freeze packaging is **`5.5.0` only** if freeze is READY.  
4. Full SemVer always (`MAJOR.MINOR.PATCH`). Never `5.5`.  
5. **At most one PR** from this branch. Base **`vc014-vision-docs`**, body **`Depends on #146`**. **Do not merge.**  
6. **No** `release.sh` publish, **no** git tag, **no** npm publish in-story.  
7. **Do not edit** VC014 evidence files or PR **#146**.  
8. Claims: **on-branch cut ≠ shipped**.  
9. **V3-60-3 is a required V3 criterion for freeze.** Prior VC011–VC014 honesty left it residual. Close **only** with narrow existing-spec behavior + **public Path A** R0A (parent mints/holds `snippet_id` → implement-class worker mutates same path → parent stale edit rejected **or** real parent table expiry observed). Thin `dsb-agent` units / docs alone are **not** enough. **No fake stamps.**  
10. If V3-60-3 (or any required V1–V4 criterion) cannot be closed → **fail closed**: **no** `5.5.0` bump, **no** vision-complete claim; land **bounded readiness-audit** evidence + one docs audit PR if useful.

---

## 1. Why this PR (one sentence)

Audit whether the vision-complete train (V1 Deep Code + V2 Reasonix + V3 Grok throughput + V4 product finish) is genuinely green on public Path A with honest residuals only; if and only if yes, cut on-branch **`5.5.0`**; otherwise document exact blockers (especially **V3-60-3**) and stop.

---

## 2. Acceptance map (V1–V4 → evidence)

### 2.1 V1 — Deep Code (L1) — stack cut **5.3.0** (VC006)

| ID | Requirement | Provenance at plan open | Freeze disposition |
|----|-------------|-------------------------|--------------------|
| **V1-45-1** | Session `snippet_id` table on Path A read | VC003–VC006 Path A R0A READY | **Expect GREEN** (cite stack; re-run only if sources change) |
| **V1-45-2** | Edit requires valid `snippet_id` | VC004 + VC006 stale-id R0A | **Expect GREEN** |
| **V1-45-3** | Write create-only; overwrite safety class | VC005 + VC006 | **Expect GREEN** |
| **V1-45-4** | Bash mutation invalidates snippets for touched paths | VC005/VC006 bash-stale R0A | **Expect GREEN** |
| **V1-90** | Perms matrix under new edit surface | Heart regression + owner-bar | **Must re-run green** |
| **V1-70** | Skills index thrash-free | Wire + units on stack | **Expect GREEN** (honest residual if any stays residual) |

### 2.2 V2 — Reasonix (L2) — VC007–VC009 (stack)

| ID | Requirement | Provenance | Freeze disposition |
|----|-------------|------------|--------------------|
| **V2-10-1** | Spec 10 stable prefix assembly | VC007 READY | **Expect GREEN** |
| **V2-10-2** | Compaction/resume no thrash | VC007 goldens | **Expect GREEN** (scope as documented) |
| **V2-15** | One-pass repair on Grok dispatch | Owner-bar / prior G007 | **Expect GREEN** via owner-bar |
| **V2-20** | Flash default + Pro sticky | Path A routing stamp | **Expect GREEN** |
| **V2-30** | `reasoning_effort` on DeepSeek wire | VC008 READY | **Expect GREEN** (CLI override residual OK) |
| **V2-cache** | User-visible or loggable cache-hit | VC009 READY | **Expect GREEN** |

### 2.3 V3 — Grok throughput (L3) — stack cut **5.4.0** (VC010–VC013)

| ID | Requirement | Provenance | Freeze disposition |
|----|-------------|------------|--------------------|
| **V3-50-1** | Multi-tool RO parallel + mutate serial | VC010 / VC013 re-prove | **Expect GREEN** |
| **V3-50-2** | Background shell + collect-by-id | VC010 / VC013 | **Expect GREEN** |
| **V3-60-1** | Explore + implement subagent Path A | VC011 / VC013 | **Expect GREEN** |
| **V3-60-2** | Worker stable prefix epoch stamp | VC011 / VC013 | **Expect GREEN** |
| **V3-60-3** | Worker mutation invalidates parent snippets | **VC011 residual** (thin unit only) | **REQUIRED** — attempt narrow close (§3); else **BLOCK freeze** |
| **V3-WT** | Worktree dogfood + bare `dsb` honesty | VC012 / VC013 (interactive create residual OK if dogfood proven) | **Expect GREEN** with known interactive residual |

### 2.4 V4 — Product finish → cut **`5.5.0`**

| ID | Requirement | Freeze disposition |
|----|-------------|--------------------|
| **V4-ver** | Isolated npm install smoke on **current latest** proves product SemVer / no false banner | **Must run this session** on npm **`5.2.2`** latest |
| **V4-plat** | Prebuilt platforms claimed in README actually ship | **Must prove** `darwin-arm64` asset on Latest (**v5.2.2** asset present at open) |
| **V4-docs** | User-guide matches behavior; KNOWN_LIMITS only true residuals | **PR #146 review** (base stack; do not edit #146) |
| **V4-owner-bar** | `./scripts/test-owner-bar.sh` exit 0 | **Must re-run** + path-linkage + heart |
| **V4-cut** | Dual adversarial review + CHANGELOG + tag `v5.Y.0` | Dual review = **separate lanes after branch ready**; in-story: CHANGELOG + bump only; **no tag/publish** |

### 2.5 Explicit non-claims at plan open

- Not vision-complete until §2.1–2.4 are genuinely green (especially **V3-60-3**).  
- Not shipped **5.5.0** (on-branch ≠ registry/GitHub).  
- Not dual review complete (external lanes).  
- Not npm/GitHub publish.  
- Not editing VC014 / #146.  
- Not inventing Path A table-expire green from thin `dsb-agent` `parent_after_worker` alone.

---

## 3. V3-60-3 close attempt (narrow only)

### 3.1 Spec / call-path facts (inspected before design)

| Layer | Fact |
|-------|------|
| Spec 60 §1.2 / T3 | Implement worker mutates → parent **snippet table expires** (product default: all) **or** touched paths |
| Thin Path B | `dsb_agent::parent_after_worker` → `snippets.expire_all()` when `mutated` — unit `implement_write_mutates` **green** — **not** Path A sole proof |
| Path A store | Grok `SessionSnippetStore` is **session-local** (`Resources` bag); mint on `read_file`; require on `search_replace`; expire on parent-side write/bash/search_replace |
| Path A spawn | Public tool `spawn_subagent` / implement-class `general-purpose`; child session has **separate** resources |
| Parent after child | **No** existing Path A hook found that calls `expire_all` / `expire_path` on parent store when implement-class child mutates |
| Content safety | Parent edit with pre-mutation `snippet_id` after **same path** disk change should hit **`snippet_stale`** (version ≠ current) even if id remains in table — per VC004/VC005 search_replace gate |

### 3.2 Acceptable close (user bar)

Public `deepseek-build` / `dsb` Path A hermetic R0A:

1. Parent mints/holds `snippet_id` for path **P**.  
2. Implement-class worker mutates **P**.  
3. Parent subsequent edit using the **pre-mutation** `snippet_id` is **rejected**, **or** real parent table expiry is observed.  
4. Wire + META under `docs/product/evidence/`; public entry (no `DEEPSEEK_BUILD_AGENT_BIN` for the claim).  

Thin units / docs alone → **do not close**.

### 3.3 Attempt strategy (ordered)

1. **R0A-first (prefer no product invent):** extend hermetic fixture + public harness scenario `parent-worker-snippet-stale` (or VC015-named):  
   - Seed `parent_seed.txt`  
   - Parent `read_file` → capture `snippet_id`  
   - `spawn_subagent` general-purpose mutates `parent_seed.txt`  
   - Parent `search_replace` with stale `snippet_id` + original old_string → expect tool error (`snippet_stale` / `snippet_not_found` / equivalent)  
   - Final token `parent-worker-snippet-stale-ok`  
2. If R0A **PASS** on public entry → record **V3-60-3 CLOSED** with **honest mechanism** (stale-edit reject vs table expire). Prefer table-expire if a **narrow** existing-spec hook is already reachable; **do not** invent a fake stamp file.  
3. If R0A cannot prove without large new spawn/completion plumbing across parent resources → **stop**; residual remains exact; freeze **BLOCKED**.

### 3.4 Out of scope for “narrow”

- Nested fleets, live multi-model sole proof, mandatory worktree for every implement worker  
- Replacing Path A store with thin `dsb-tools` executor  
- Claiming thin `parent_after_worker` unit as Path A sole green  
- Broad vendor refactors unrelated to Spec 60 T3

---

## 4. PR units (single stacked PR; atomic commits)

**One PR only** from `vc015-freeze-audit`, base **`vc014-vision-docs`**, body **`Depends on #146`**.

### PR unit 1 — `docs(product): VC015 vision freeze plan and floor`

- **Intent:** Plan-first gate before any source/version edit.  
- **Touches:** this file only.  
- **Depends on:** VC014 tip / #146 @ `58aff64`.  
- **SemVer:** none.  
- **Tests:** n/a (docs).

### PR unit 2 — `test(scripts): Path A R0A parent snippet after worker mutate` *(conditional)*

- **Intent:** Hermetic fixture + public-entry harness for V3-60-3.  
- **Touches:** `scripts/lib/scripted_deepseek_server.py`, `scripts/test-path-a-vc015-r0a.sh` (or extend VC011 harness narrowly).  
- **Depends on:** unit 1.  
- **SemVer:** none.  
- **Tests:** new/extended Path A R0A script exit 0.

### PR unit 3 — `feat(tools)|fix(agent): Path A parent snippet invalidate after worker` *(conditional; only if R0A needs narrow product wiring)*

- **Intent:** Existing-spec parent invalidation after implement-class mutation on Path A only if required for proof.  
- **Touches:** minimal Path A / vendored Grok call site only if unavoidable.  
- **Depends on:** unit 2 failing without it.  
- **SemVer:** none.  
- **Tests:** unit 2 R0A green; owner-bar / path-linkage / heart stay green.

### PR unit 4a — freeze path only if all V1–V4 green

`chore(release): bump product to 5.5.0` via `./scripts/bump-version.sh 5.5.0` **exactly once** + CHANGELOG / versions / README as script requires + CUT sections in this evidence file.

### PR unit 4b — fail-close path if any required criterion residual

`docs(product): VC015 readiness audit BLOCKED (no 5.5.0)` — fill this file with exact V3-60-3 residual, V4-ver/V4-plat results, no-freeze verdict; **no** SemVer bump.

### PR unit 5 — gates + English PR open

- `git diff --check`  
- `./scripts/check-semver.sh`  
- `./scripts/reorder-changelog.sh --check` (if CHANGELOG touched)  
- `./scripts/check-path-a-linkage.sh`  
- `./scripts/test-owner-bar.sh` (restore TSV)  
- `./scripts/test-heart-regression.sh` (restore TSV)  
- Relevant Path A R0A  
- Public English gate + labels; **do not merge**

## Sequential

1. unit 1 (this plan) →  
2. V4-ver / V4-plat probes + V1–V4 paper audit →  
3. unit 2 R0A attempt → unit 3 only if needed →  
4. unit 4a **or** 4b → unit 5 → open **one** PR

## Parallel (safe)

- V4-ver npm smoke ∥ V4-plat asset check ∥ paper audit of VC006–VC014 (after unit 1)  
- owner-bar / heart after code units (not during disk-critical R0A if space constrained)

---

## 5. V4 probes (to fill during story)

### 5.1 V4-ver — isolated npm install smoke (current latest)

| Check | Plan |
|-------|------|
| Install | Isolated prefix / temp dir: `npm pack` or `npm i -g @innocarpe/deepseek-build@5.2.2` into clean prefix |
| SemVer | `deepseek-build --version` and `dsb --version` report product **`5.2.2`** (full form) |
| Banner | No false “update available” claiming a wrong newer product line when already on latest |
| Dual CLI | Both bins present and same version string |

### 5.2 V4-plat — release assets vs claims

| Check | Plan |
|-------|------|
| Claimed platform | README / `docs/user-guide/05-npm.md` / ADR 0009: **`darwin-arm64` only** |
| Asset | `gh release view v5.2.2` → `deepseek-build-5.2.2-darwin-arm64.tar.gz` (observed at open) |
| Honesty | Do not claim multi-arch prebuilts without assets |

### 5.3 V4-docs — PR #146

| Check | Plan |
|-------|------|
| Review surface | Open PR **#146** body + user-guide 10–14 + KNOWN_LIMITS residual allow-list |
| Residual honesty | V3-60-3 still residual **until** this story closes it with Path A R0A |
| Non-edit | Do not push to `vc014-vision-docs` or amend #146 |

### 5.4 V4-owner-bar

| Gate | Command |
|------|---------|
| Owner-bar | `./scripts/test-owner-bar.sh` |
| Path linkage | `./scripts/check-path-a-linkage.sh` |
| Heart | `./scripts/test-heart-regression.sh` |
| SemVer shape | `./scripts/check-semver.sh` |

Restore generated TSVs; clean exact target trees if disk is low.

---

## 6. Verdict scaffold (fill at close)

| Field | Value (fill later) |
|-------|---------------------|
| **Freeze** | `READY` \| `BLOCKED` |
| **SemVer action** | `bump 5.5.0` \| `none` |
| **V3-60-3** | `CLOSED (Path A R0A …)` \| `RESIDUAL (exact reason)` |
| **V4-ver** | `PASS` \| `FAIL` + notes |
| **V4-plat** | `PASS` \| `FAIL` + notes |
| **V4-docs** | `PASS (#146)` \| `FAIL` |
| **V4-owner-bar** | `PASS` \| `FAIL` |
| **PR URL** | _(after open)_ |
| **Dual review** | external — not self-served |

---

## 7. Stack / PR open checklist

- [ ] Branch `vc015-freeze-audit` based on `vc014-vision-docs` @ `58aff64`  
- [x] First commit = this plan (before source/version edits)  
- [ ] Atomic commits per §4  
- [ ] Clean tree before PR  
- [ ] `gh pr create --base vc014-vision-docs` English body + labels  
- [ ] Body includes **Depends on #146**  
- [ ] `~/.local/bin/gh-public-english-gate`  
- [ ] `gh pr view --json title,labels,baseRefName,headRefName,url`  
- [ ] **Do not merge**; **do not tag/publish**  
- [ ] Report READY vs BLOCKED + exact PR URL  

---

## 8. READY / BLOCKED evidence (filled later)

_(Sections below are intentionally empty at plan commit.)_

### 8.0 Provenance

| Field | Value |
|-------|--------|
| Plan-first commit | _(this commit)_ |
| V3-60-3 R0A head | TBD |
| Freeze / audit head | TBD |

### 8.1 Gate log

| Command | Result | Notes |
|---------|--------|-------|
| `git diff --check` | TBD | |
| `./scripts/check-semver.sh` | TBD | |
| `./scripts/check-path-a-linkage.sh` | TBD | |
| `./scripts/test-owner-bar.sh` | TBD | |
| `./scripts/test-heart-regression.sh` | TBD | |
| Path A V3-60-3 R0A | TBD | |
| V4-ver npm smoke | TBD | |
| V4-plat assets | TBD | |

### 8.2 Criterion matrix (final)

| ID | Result |
|----|--------|
| V1-* | TBD |
| V2-* | TBD |
| V3-50-* / V3-60-1/2 / V3-WT | TBD |
| **V3-60-3** | TBD |
| V4-ver / plat / docs / owner | TBD |

### 8.3 Residuals at close

| Residual | Status |
|----------|--------|
| TBD | TBD |

### 8.4 Explicit non-claims at close

- TBD
