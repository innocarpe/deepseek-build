# Bundled skills

Product-shipped Agent Skills (`SKILL.md` directories) will live here.

Discovery will also include project/user paths (Deep Code–compatible ideas):

- Project: `.deepseek-build/skills/`, `.agents/skills/`
- User: `~/.deepseek-build/skills/`, `~/.agents/skills/`

Exact precedence → `docs/specs/70-…` (TODO).
